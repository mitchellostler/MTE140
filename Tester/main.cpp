#include <iostream>

int day_of_week(int year, int month = 1, int day = 1) {
    return 0;
}

int main() {
       std::cout << day_of_week(1995);
       std::cout << day_of_week(1995, 10);
       std::cout << day_of_week(1995, 10, 4);
    std::cout << "HELLO WORLD";
}